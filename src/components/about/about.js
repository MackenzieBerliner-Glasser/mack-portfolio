import React, { Fragment } from 'react';
import MainHeader from '../main-header/main-header';

const About = () => {
  return (
    <Fragment>
      <MainHeader />
      <h1>{'<About />'}</h1>
    </Fragment>
  );
};

export default About;
